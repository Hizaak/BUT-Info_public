#include "Carre.h"
#include "Couleur.h"
#include "Fenetre.h"
#include "Souris.h"

int main(int argc, char **argv)
{
    gtk_init(&argc,&argv);

    Fenetre f;
    Souris s;
    f.apparait("Test TD2",600,400,0,0,200,200,200);
    s.associerA(f);
    Carre(40, 40, 30);
    int x,y,b:
    while(true)
    {
    if(s.testeBoutons(x,y,b)){
    if (x>)}
    }
    return 0;
}
